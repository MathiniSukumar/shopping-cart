import { SelectedProductsPipe } from './selected-products.pipe';

describe('SelectedProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
